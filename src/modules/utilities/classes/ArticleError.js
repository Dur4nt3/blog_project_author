export default class ArticleError {
    constructor(formLevel, title, description, body) {
        this.formLevel = formLevel;
        this.title = title;
        this.description = description;
        this.body = body;
    }
}