export default class ArticleData {
    constructor(title, description, body, createdAt, lastModification) {
        this.title = title;
        this.description = description;
        this.body = body;
        this.createdAt = createdAt;
        this.lastModification = lastModification;
    }
}